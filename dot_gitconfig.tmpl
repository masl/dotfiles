[user]
	name = {{ .git.name }}
	email = {{ .git.email }}
	signingkey = {{ .git.signingkey }} 

[alias]
	graph = log --all --graph --decorate --oneline
	branches = branch -a
	remotes = remote -v
	whoami = config user.email

[commit]
	gpgsign = true

[gpg]
	program = /bin/gpg2
